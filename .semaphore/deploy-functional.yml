version: v1.0
name: Deploy to stubbed functional
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Deploy to production
    task:
      secrets:
        - name: GCP
      jobs:
        - name: run deploy
          commands:
            - gcloud auth activate-service-account --key-file=.helloworldkey.json
            - gcloud auth configure-docker -q
            # Deploy to Cloud Run, using flags to avoid interactive prompt:
            - gcloud run deploy semaphore-example-dep --project advance-age-269909 --image asia.gcr.io/advance-age-269909/semaphore-example:${SEMAPHORE_GIT_SHA:0:7} --region europe-west4 --platform managed
  - name: ft
      dependencies: ["Deploy to production"]
      task:
        jobs:
          - name: run fts
            commands:
              -